import React from 'react';
import { View, Text } from 'react-native';

/* eslint-disable-next-line */
export interface ReactNativeKitProps {
}

export function ReactNativeKit(props: ReactNativeKitProps) {
  return (
    <View>
      <Text>Welcome to react-native-kit!</Text>
    </View>
  );
}

export default ReactNativeKit;
